.DS_Store
*.log

# Node toolchain (lint/spell only; no runtime deps)
node_modules/
npm-debug.log*

# Editor
.vscode/
.idea/

# Local OpenSpec test fixture (throwaway; recreate with `openspec init`) — used as
# a live test bed by the harness verification plan, never committed or linted.
ospec-test/
